#!/bin/sh
# Assemble and run the MSX TV hub container (http://tv.lan for the LG TV).
# This script is intended to run on the MSX host `lzz` (access via `ssh lzz`).
exec /usr/bin/docker compose -f "$HOME/.local/share/msx-hub/compose.yaml" up -d --remove-orphans "$@"
